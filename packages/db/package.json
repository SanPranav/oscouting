{
  "name": "@3749/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/client.js",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev --name init",
    "seed": "node prisma/seed.js",
    "build": "echo \"db build not required\""
  },
  "dependencies": {
    "@prisma/client": "^6.6.0"
  },
  "devDependencies": {
    "prisma": "^6.6.0"
  }
}
